<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_game_projet_tir_au_but">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_game_projet_tir_au_but">
        <activity
            android:name=".controllerView.ManuelUtilisation"
            android:exported="false" />
        <activity
            android:name=".controllerView.TableauScore"
            android:exported="true" />
        <activity
            android:name=".controllerView.Game"
            android:exported="true" />
        <activity
            android:name=".controllerView.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>